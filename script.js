//let numButtonClicks = 0;
function buttonClicked() {
    //numButtonClicks = numButtonClicks + 1;
    document.getElementById("exbuttonDiv").textContent =
    //    "And a " + numButtonClicks;
        "Because they don't have the guts!";
}
